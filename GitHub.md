# GitHub

## 一. 介绍

Github项目托管网站，大量的企业级工程和开源工程在网站云端托管，聚集和海量项目，软件研发工程师的工具网站

1. **仓库**:是github中项目(工程)的存储单位，一个用户可以创建多个仓库，每个仓库存一个项目
2. **分支**:资源的存储单位，一般一个仓库中有一个默认的**主分支(Master)**，上传代码数据默认放到主分支中,仓库包含分支，一个仓库可以有多个分支，主分支只有一个

### 关于查询

按用户关键进行全站的模糊查询，注意下面的语言标签

标签查询  (样本)xxx `sample`   (资料)xxx `tutorial`

### 关于仓库中的项目

1. **code**:存储工程资源数据，代码、配置文件等等

​       code中重要的文件：README.md  LICENSE

​       README.md：文本修饰语言，文件后缀统一为.md，使用修饰符与正文结合变为一个很好的效果

​       LICENSE：关于许可证，GPL3.0 APHACHE 2.0 MIT 给使用者最大使用权力，最小的限制，<br>       如果不是知名机构许可证，要详细阅读条款(避免法务问题）

2. **issues**：问答板块，提问和解决问题

*****

## 二. GitHub基本配置

用个人电脑，向云端(github仓库)上传数据，需要进行设备认证，让github账户任务此设备是受信任的，后续完成加密传输

### 1. 设备认证

1. 创建一个本地仓库 **git init**

​       当前位置有(Master)表示再仓库所在位置，后续的说有操作都是在这个位置

​       **git config -list**  查看git配置文件信息

2. 在配置文件中添加两条配置，关于email和 username

​       **git config -global user.name "用户名"**

​       **git config -global useremail "邮箱"**

3. 查看是否配置成功

   **ssh -T git@github.com**

4. 密钥生成，创建本机RSA非对称加密密钥，并且传给账户，后续使用此密钥完成数据加密传输

   **ssh-keygen -t rsa -C "邮箱"**

   记录一下密钥文件的存储位置，等会去复制密钥串

   粘贴位置，点击头像--> menu--> Settings--> ssh and GPG keys--> new ssh key-->填写密钥名-->粘贴密钥-->add ssh key

### 2. 为仓库地址创建别名

**git remote add origin "ssh地址"**   为云端仓库ssh地址创建别名 叫origin

**git remote remove origin**              删除origin地址别名

*****

## 三. 关于项目管理（内容的上传下载）

使用**git bash**进行本地内容的上传，通过命令方式数据更新/版本更新

可以以文件或文件夹为单位进行操作

1. 资源文件（test.c）-->添加到缓冲区 

   操作：

   **git add file**         资源添加到缓冲区

   **git rm file**           资源从缓冲区删除同时删除文件

   **git restore file**   将资源缓冲区还原到磁盘中 

2. git缓冲区--> 提交到git本地仓库  

   操作：

   **git status**                                           查看缓冲区状态

   **git commit -m "提交说明"**              将缓冲区数据提交到本地仓库 可以附加说明信息

3. git本地仓库 -->   将本地仓库数据Push到云端  版本更新 本地的新版本 同步合并云端的旧版本 

   操作：

   **git push origin master**    将本地仓库主分支数据 推到origin指向的云端仓库中

4. 云端仓库：上传的分支与云端仓库主分支同名 进行合并替换

   ​                    不同名 云端仓库创建新分支 保存用户上传内容